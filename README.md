# Bot

**An agentic harness for everyone.**

Bot provides the structure in which people and agents work together. It is designed to adapt to different people, agents, and kinds of work instead of being defined by a fixed set of capabilities.

## Development

```bash
npm run setup
npm run dev
```

Use `npm run help` for the current command guide. `npm run dev` starts the browser application and the Electron application together with their services. `npm run check` runs repository lint, typecheck, tests, and builds; `npm run verify` adds integration checks. Turbo owns JavaScript/TypeScript tasks and `uv` owns Python dependencies.

## Documentation

See the [developer documentation](docs/index.md) for the product direction, architecture, setup, and module boundaries.
