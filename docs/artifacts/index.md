# Artifacts

Final, user-facing HTML visualizations of the project live here.

| Artifact | Purpose |
| --- | --- |
| [Cloud agent architecture](cloud-agent-architecture.html) | Implemented agent capabilities, durable state, execution flow, and remaining boundaries. |

## Screenshot provenance

The unchanged PNGs in `agent-runtime/` come from the [foundation review](https://github.com/cekrauseee/bot/pull/8), revision `392c5fb`. They show the real local API, PostgreSQL, and WebSocket flow driven by deterministic AI events, at desktop and 320-pixel mobile widths in light and dark appearances. They are historical UI evidence, not newly captured screenshots or proof of live model/Sandbox execution. Exact capture timestamps were not recorded in the image catalog.
